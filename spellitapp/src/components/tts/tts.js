//all tts things here
//<button onClick={() => {window.responsiveVoice.speak('Just a small test','US English Male')}}>Speak!</button>

import React from 'react';

class TextToSpeech extends React.Component{
    render(){
        return(
            <div>

            </div>
        )
    }
}

export default TextToSpeech;
